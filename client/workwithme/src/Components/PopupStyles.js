const popupContent = {
    textAlign: "center",
    height: "160px",
    // marginTop: "30px",
    width: "130px",
    
   };
  const popupHead = {
    fontWeight: "bold",
    fontSize: "13px",
  };
  
  const popupText = {
    fontSize: "10px",
    marginBottom: "20px"
  };
  
  const okText = {
    fontSize: "15px"
  };
  
  export { popupContent, popupHead, popupText, okText };