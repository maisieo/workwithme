const popupContent = {
    textAlign: "center",
    height: "120px",
    // marginTop: "30px",
    width: "110px",
    
   };
  const popupHead = {
    fontWeight: "bold",
    fontSize: "10px",
  };
  
  const popupText = {
    fontSize: "10px",
    marginBottom: "20px"
  };
  
  const okText = {
    fontSize: "15px"
  };
  
  export { popupContent, popupHead, popupText, okText };